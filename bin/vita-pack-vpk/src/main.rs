fn main() {
    let args: Vec<String> = std::env::args().collect();
    let status: i32 = vita_pack_vpk(&args);
    std::process::exit(status);
}

fn vita_pack_vpk(args: &Vec<String>) -> i32 {
}
    return 0;
}
